<h1 class="m-1 p-1">My tasks</h1>

<div>
  <% @tasks.each do |task| %>
  <br>
    <%= link_to task_path(task), class:"text-decoration-none" do %>
    <input type="checkbox" <% if task.completed %> checked <%end%>>
     <%= "#{task.title} •"%>
      <%= link_to destroy_path(task), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}, class:"text-decoration-none" do %>
      <i class="fa-solid fa-trash-can" style="color: #0086b3;"></i>
      <% end %>
    <%end%>
  <% end %>
</div>
<br>
<%= link_to "Add a new task", new_path, class:"text-decoration-none"%>
